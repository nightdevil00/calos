#!/bin/bash

if pgrep -x waybar >/dev/null; then
  pkill -x waybar
  sleep .25
  systemctl --user start waybar.service >/dev/null 2>&1 &
else
  systemctl --user start waybar.service >/dev/null 2>&1 &
fi
